name: Frontend CD

on:
    push:
        branches: [main]
    workflow_dispatch:

permissions:
    contents: write

jobs:
    build-and-test:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v6

            - name: Set up Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '22'

            - name: Install dependencies
              run: npm install --force
              working-directory: ./KaktusWikiUi

            - name: Build project
              run: npm run build --if-present
              working-directory: ./KaktusWikiUi

            - name: Fix hard coded linking to relative paths
              run: |
                  sed -i 's/\/assets/\.\/assets/g' ./KaktusWikiUi/dist/index.html
                  sed -i 's/\/Wordle\/assets/\.\/assets/g' ./KaktusWikiUi/dist/index.html

            - name: Deploy to GitHub Pages
              uses: peaceiris/actions-gh-pages@v3
              with:
                  github_token: ${{ secrets.GITHUB_TOKEN }}
                  publish_dir: ./KaktusWikiUi/dist
                  user_name: 'github-actions[bot]'
                  user_email: 'github-actions[bot]@users.noreply.github.com'
                  keep_files: false
                  commit_message: 'Deploy to GitHub Pages'